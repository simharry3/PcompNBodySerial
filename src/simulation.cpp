#include "nBody.h"
#include <system.h>


void calculateSystem() {
	
}

int main(){
    int numBodies  = 1000;
    int steps = 1000;
    system sys;
    sys.initSystem(numBodies);
    while(n < steps){
        //calculateSystem();
        //stepSystem();
    }
}
